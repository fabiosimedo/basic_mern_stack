# Basic MERN-STACK back end

This is a simple CRUD with free mongodb atlas tier

<code>npm install express cors dotenv mongoose</code>
<br>
<code>npm install -D nodemon</code>

<quote>OBS:. RactJs is not mandatory in the front end. You might choose other technologies if you want to.</quote>